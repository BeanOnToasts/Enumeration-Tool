# Summary
Scan most commonly used ports on the user's IP and return a list of which ports are open
# Functionality
* Scans most common ports (around 30)
* Returns list of all the ports that were found to be open
## Notes
* In the unittests, a resourcewarning is indicated for every port that is scanned, however the program still runs and passes the test