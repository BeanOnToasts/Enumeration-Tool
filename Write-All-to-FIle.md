# Summary
Runs all functions and writes the information to the file
# Functionality
* Collects the values from every enumeration function and the hashed passwords and writes to a formatted CSV file
## Notes
* Can only get hashed passwords on Linux
* This can be ran before the other functions as it runs them separately